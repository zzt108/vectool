<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Vectool.UI.WinUI.RecentFilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top filter bar: parity with WinForms filters (All/Linked/Unlinked/Specific) -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Padding="8">
            <ComboBox x:Name="Filter"
                      Width="220"
                      PlaceholderText="Filter"
                      SelectionChanged="Filter_SelectionChanged">
                <ComboBoxItem Content="All" Tag="All" IsSelected="True"/>
                <ComboBoxItem Content="Linked" Tag="Linked"/>
                <ComboBoxItem Content="Unlinked" Tag="Unlinked"/>
                <ComboBoxItem Content="Specific" Tag="Specific"/>
            </ComboBox>

            <ComboBox x:Name="SpecificStore"
                      Width="260"
                      PlaceholderText="Specific store"
                      IsEnabled="False"
                      SelectionChanged="SpecificStore_SelectionChanged" />

            <Button x:Name="RefreshBtn"
                    Content="Refresh"
                    Click="RefreshBtn_Click" />
        </StackPanel>

        <!-- Files grid: mirrors Recent Files list with name/path/store -->
        <GridView x:Name="Files"
                  Grid.Row="1"
                  IsItemClickEnabled="True"
                  SelectionMode="Single"
                  ItemClick="Files_ItemClick"
                  DoubleTapped="Files_DoubleTapped"
                  Padding="8">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:RecentFileItem">
                    <Grid Padding="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="5*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind FileName}" Grid.Column="0" />
                        <TextBlock Text="{x:Bind Path}" Grid.Column="1" TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{x:Bind LinkedStoreName}" Grid.Column="2" />
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <!-- Bottom bar: parity utilities -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12" Padding="8">
            <TextBlock Text="Tip: Double-click to open, right-click for actions." />
        </StackPanel>
    </Grid>
</Page>

<!--<Window
    x:Class="VecTool.UI.WinUI.RecentFilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VecTool.UI.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="RecentFilesPage">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

    </Grid>
</Window>-->
<!-- File: Vectool.UI.WinUI/Pages/RecentFilesPage.xaml -->