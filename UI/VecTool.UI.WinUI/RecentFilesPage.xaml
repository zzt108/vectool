<?xml version="1.0" encoding="utf-8"?>
<!-- File: UI/VecTool.UI.WinUI/RecentFilesPage.xaml -->
<Page
    x:Class="VecTool.UI.WinUI.RecentFilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:VecTool.UI.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:recentfiles="using:VecTool.Core.RecentFiles"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top filter bar (parity with WinForms filters: All/Linked/Unlinked/Specific) -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="8" Spacing="12">
            <!-- 🔄 MODIFIED: Removed SelectionChanged attribute -->
            <ComboBox x:Name="Filter" Width="220" PlaceholderText="Filter">
                <ComboBoxItem Content="All" IsSelected="True" Tag="All" />
                <ComboBoxItem Content="Linked" Tag="Linked" />
                <ComboBoxItem Content="Unlinked" Tag="Unlinked" />
                <ComboBoxItem Content="Specific" Tag="Specific" />
            </ComboBox>

            <!-- 🔄 MODIFIED: Removed SelectionChanged attribute -->
            <ComboBox x:Name="SpecificStore" Width="260" IsEnabled="False" PlaceholderText="Specific store">
            </ComboBox>

            <Button x:Name="RefreshBtn" Click="RefreshBtn_Click" Content="Refresh" />
        </StackPanel>

        <!-- Files grid (mirrors Recent Files list with name/path/store) -->
        <GridView 
            x:Name="Files" 
            Grid.Row="1" 
            DoubleTapped="FilesDoubleTapped" 
            IsItemClickEnabled="True" 
            ItemClick="FilesItem_Click" 
            Padding="8" 
            SelectionMode="Single">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="recentfiles:RecentFileItem">
                    <Grid Padding="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="5*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{x:Bind FileName}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind Path}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="2" Text="{x:Bind LinkedStoreName}" />
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <!-- Bottom bar (parity utilities) -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Padding="8" Spacing="12">
            <TextBlock Text="Tip: Double-click to open, right-click for actions." />
        </StackPanel>
    </Grid>
</Page>
